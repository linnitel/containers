
#ifndef ITERATOR_LIBRARY_HPP
# define ITERATOR_LIBRARY_HPP

namespace ft {
//private:
//pointer _it;
//public:
//pointer operator->() { return _it; }
//Iterator operator++(int) {
//	Iterator tmp = *this;
//	++(*this);
//	return tmp;
//};
//
//friend bool operator== (const Iterator<T>& a, const Iterator<T>& b);
//friend bool operator!= (const Iterator<T>& a, const Iterator<T>& b);
//
//};
//
//template <class T, class Pointer = T*, class Reference = T&>
//bool operator== (const Iterator<T, Pointer, Reference>& a, const Iterator<T, Pointer, Reference>& b) { return a._it == b._it; }
//template <class T, class Pointer = T*, class Reference = T&>
//bool operator!= (const Iterator<T, Pointer, Reference>& a, const Iterator<T, Pointer, Reference>& b) { return a._it != b._it; }
}
#endif //ITERATOR_LIBRARY_HPP
